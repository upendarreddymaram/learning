# Prometheus

Metrics collection configuration.

## Phase 10

Track: API latency, error rate, queue depth, worker saturation, CI success rate, agent failure rate, tokens, AI cost.

Add `prometheus.yml` when enabling the observability stack in docker-compose.
