# Tests

| Directory | Scope |
|-----------|-------|
| `unit/` | Unit tests per service |
| `integration/` | Cross-service integration tests |
| `e2e/` | End-to-end: ClickUp webhook → PR → CI |

Run tests from each service initially; consolidate shared fixtures here as the project grows.
