# Events Package

Event schemas, publishers, and consumers.

## Phase 7+

Event types: `task.created`, `agent.started`, `pr.created`, `ci.completed`, etc.

Schema versioning and compatibility rules documented here.
